export const showSelectGroupSelector = (state) => state.med.showSelectGroup;
export const showSelectVacSelector = (state) => state.med.showSelectVac;
export const showBurgerModalSelector = (state) => state.med.showBurgerModal;
export const showRedactModalSelector = (state)=> state.med.showRedactModal;
export const listSelector = (state)=>state.med.list
export const tokenSelector = (state)=>state.med.token
export const selectIdSelector = (state)=>state.med.selectId
export const reductStudentSelector = (state)=>state.med.reductStudent
export const showLoginModalSelector = (state)=> state.med.showLoginModal;
export const schoolIdSelector = (state)=> state.med.schoolId;

